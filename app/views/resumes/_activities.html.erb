<div class="activities-block">
	<div class="activity-header">
		<h3>Activities</h3>
	</div>
	<div class="activity-body">
		<% @activities.each do |extracurricular| %>
			<div class="activity">
				<ul>
					<li>
						<span class="activity-organization">
							<%= extracurricular.organization %>
						</span>
						<span class="activity-date"> (<%= extracurricular.start_date.strftime("%m/%Y") %>
							<% if extracurricular.start_date.strftime("%m/%Y") != extracurricular.end_date.strftime("%m/%Y") %>
								- <%= extracurricular.end_date.strftime("%m/%Y") %>
							<% end %>
							)
						</span>
					</li>
				</ul>
			</div>
		<% end %>
	</div>
	<br class="clrFLoat" />
</div>
