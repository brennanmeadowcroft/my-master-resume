<div class="span4">
    <h4>Choose Tags</h4>
    <% @tags.each do |tag| %>
        <%= check_box_tag "education[tag_ids][]", tag.id, @education.tags.include?(tag) %>
        <%= tag.description %>
    <% end %>
    <p><%= link_to "Create A New Tag", new_tag_path %></p>
</div>